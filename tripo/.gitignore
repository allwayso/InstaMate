# Tripo 管线相关（凭据、产物、输入素材）—— 放在这里而不是根目录，
# 因为这些规则全部只对 tripo/ 有意义。

# ★ API 密钥：绝不能入库
.env
.env.*

# 管线产物：模型 / 贴图 / 中间图 / 状态文件，体积大且都可由原始照片重新生成
output/

# 上传探测器的临时记录
.upload_probe.json

# 输入照片：随手丢进来的原图（管线会把它重画成 T-pose 参考图，属于中间物）
*.jpg
*.jpeg
*.png
*.webp

# Python 缓存
__pycache__/
.venv/
